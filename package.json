{
  "name": "apnakissan-ecosystem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix Backend && npm install --prefix Admin && npm install --prefix User",
    "dev:backend": "npm run dev --prefix Backend",
    "dev:admin": "npm run dev --prefix Admin",
    "dev:user": "npm run start --prefix User"
  }
}
